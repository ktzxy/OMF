# 强制脚本/配置/SQL 使用 LF 换行, 避免 Windows 检出后 CRLF 导致 Linux 上 bash 执行失败
* text=auto
*.sh text eol=lf
*.conf text eol=lf
*.sql text eol=lf
*.md text eol=lf

# 取消过滤器继承(防止全局 Git LFS 过滤器在 checkout 时把文件清空)
* -filter
